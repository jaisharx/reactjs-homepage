import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  colors: {
    brand: {
      400: '#61dafb',
      500: '#20232A',
    },
    shades: {
      400: '#373940'
    }
  },
})

export default theme
